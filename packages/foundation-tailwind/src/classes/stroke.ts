export type TTailwindStrokePrefix = "stroke";

export type TTailwindStrokeValue =
  "none" | "standard" | "thin" | "thick" | "thicker";

export type TTailwindStrokeClass =
  `${TTailwindStrokePrefix}-${TTailwindStrokeValue}`;
